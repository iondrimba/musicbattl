define([], function () {
    var ForgotPassword = Backbone.View.extend({
        el: ".forgot-password",
        events: {
        },
        initialize: function () {
        },
        render: function () {
        }
    });

    return ForgotPassword;
});